import {styled} from "@mui/material/styles";
import TableRow from '@mui/material/TableRow';

export const TableRowHead = styled(TableRow)(({ theme }) => ({
    borderBottom: '1px solid rgba(26,24,130,0.3);',
}));